sudo apt-get install curl

curl -L https://get.rvm.io | bash -s stable --ruby #エラーがでる。

curl -sSL https://rvm.io/mpapis.asc | gpg --import - #署名鍵取得
curl -sSL https://rvm.io/pkuczynski.asc | gpg --import - #署名鍵取得

curl -L https://get.rvm.io | bash -s stable --ruby

